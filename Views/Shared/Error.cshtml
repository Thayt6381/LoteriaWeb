@model ErrorViewModel
@{
    ViewData["Title"] = "Erro";
}

<h1 class="text-danger">Erro</h1>
<h2 class="text-danger">Ocorreu um erro ao processar sua solicitação.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID da Requisição:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Ambiente de Desenvolvimento</h3>
<p>
    Alterar para o ambiente <strong>Development</strong> exibirá mais informações detalhadas sobre o erro ocorrido.
</p>
<p>
    <strong>O ambiente Development não deve ser habilitado em aplicações em produção.</strong>
    Isso pode exibir informações sensíveis sobre exceções para os usuários finais.
    Para depuração local, defina a variável de ambiente <strong>ASPNETCORE_ENVIRONMENT</strong> como <strong>Development</strong> e reinicie a aplicação.
</p>
